# Método Hakari — Landing de vendas

Site estático (Cloudflare Pages ready). `pages_build_output_dir = "."`

- `index.html` — versão **Sakura** (publicada por padrão)
- `version-mauve.html` — versão Mauve (vinho/dourado)
- `version-nude.html` — versão Nude (champagne)

As imagens e vídeos carregam de `esp.metodohakari.com`; o checkout aponta para a Hotmart.
Para publicar: Cloudflare Pages → Connect to Git (este repo) ou Direct Upload desta pasta.
